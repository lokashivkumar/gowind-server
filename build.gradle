apply plugin: 'idea'
apply plugin: 'java'

allprojects {
    apply plugin: 'idea'
    apply plugin: 'project-report'

    if (System.properties['ignoreTestFailures'] == 'true') {
        test.ignoreFailures=true
    }
}